<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EMSI_Recrutement">

        <activity
            android:name=".ProfileCandidateActivity"
            android:exported="false" />

        <activity
            android:name=".CareerTipsActivity"
            android:exported="false" />

        <activity
            android:name=".JobDetailsActivity"
            android:exported="false" />

        <activity
            android:name=".MyApplicationsActivity"
            android:exported="false" />

        <activity
            android:name=".DashboardCandidateActivity"
            android:exported="false" />

        <activity
            android:name=".SavedJobsActivity"
            android:exported="false" />

        <activity
            android:name=".UploadCVActivity"
            android:exported="false" />

        <activity
            android:name=".MyCVsActivity"
            android:exported="false" />

        <activity
            android:name=".JobSearchActivity"
            android:exported="false" />

        <activity
            android:name=".RegisterCandidateActivity"
            android:exported="true" />

        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

    </application>

</manifest>
